[Service]
# Opt-in workaround: MPD 0.24.6 on some kernels accounts the idle io_uring
# waiter as continuous I/O wait. EPERM forces MPD's blocking-I/O fallback.
SystemCallErrorNumber=EPERM
SystemCallFilter=~io_uring_setup io_uring_enter io_uring_register
